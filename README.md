#### One of tests work result when defined 'DUMP_OUTPUT' macros:

![RunTestExample](RunTestExample.png)

where `S` is Split, `F` is Free and `R` is Reserved block statuses.

Test source code:

![TestSource](TestSource.png)